<section class="flat-spacing-3">
  <div class="container">
    <div class="flat-animate-tab">
      <div class="flat-title-tab-categories wow fadeInUp text-center">
        <h4 class="title">{{ section.settings.title }}</h4>
        {% if section.settings.collection_1 != blank or section.settings.collection_2 != blank %}
          <ul class="menu-tab-line justify-content-center" role="tablist">
            {% if section.settings.collection_1 != blank %}
              <li class="nav-tab-item" role="presentation">
                <a href="#collection-1" class="tab-link active" data-bs-toggle="tab">{{ section.settings.collection_1.title }}</a>
              </li>
            {% endif %}
            {% if section.settings.collection_2 != blank %}
              <li class="nav-tab-item" role="presentation">
                <a href="#collection-2" class="tab-link {% if section.settings.collection_1 == blank %}active{% endif %}" data-bs-toggle="tab">{{ section.settings.collection_2.title }}</a>
              </li>
            {% endif %}
          </ul>
        {% endif %}
      </div>
      <div class="tab-content">
        {% if section.settings.collection_1 != blank %}
          <div class="tab-pane active show" id="collection-1" role="tabpanel">
            <div dir="ltr" class="swiper tf-swiper" 
              data-swiper='{
                "slidesPerView": 2,
                "spaceBetween": 12,
                "speed": 800,
                "observer": true,
                "observeParents": true,
                "slidesPerGroup": 2,
                "pagination": { 
                  "el": ".sw-pagination-1", 
                  "clickable": true 
                },
                "breakpoints": {
                  "575": { 
                    "slidesPerView": 3, 
                    "spaceBetween": 12,
                    "slidesPerGroup": 2 
                  },    
                  "768": { 
                    "slidesPerView": 4, 
                    "spaceBetween": 24, 
                    "slidesPerGroup": 3 
                  },
                  "1200": { 
                    "slidesPerView": 6, 
                    "spaceBetween": 64, 
                    "slidesPerGroup": 3
                  }
                }
              }'>
              <div class="swiper-wrapper">
                {% for product in section.settings.collection_1.products limit: 6 %}
                  <div class="swiper-slide">
                    <div class="wg-cls style-circle hover-img">
                      <a href="{{ product.url }}" class="image img-style d-block">
                        {% if product.featured_image %}
                          <img src="{{ product.featured_image | img_url: 'master' }}"
                               data-src="{{ product.featured_image | img_url: 'master' }}"
                               alt="{{ product.title | escape }}" class="lazyload">
                        {% else %}
                          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
                        {% endif %}
                      </a>
                      <div class="cls-content text-center">
                        <a href="{{ product.url }}" class="link text-md fw-medium">{{ product.title }}</a>
                      </div>
                    </div>
                  </div>
                {% endfor %}
              </div>
              <span class="d-flex d-xl-none sw-dot-default sw-pagination-1 justify-content-center"></span>
            </div>
          </div>
        {% endif %}
        {% if section.settings.collection_2 != blank %}
          <div class="tab-pane {% if section.settings.collection_1 == blank %}active show{% endif %}" id="collection-2" role="tabpanel">
            <div dir="ltr" class="swiper tf-swiper" 
              data-swiper='{
                "slidesPerView": 2,
                "spaceBetween": 12,
                "speed": 800,
                "observer": true,
                "observeParents": true,
                "slidesPerGroup": 2,
                "pagination": { 
                  "el": ".sw-pagination-2", 
                  "clickable": true 
                },
                "breakpoints": {
                  "575": { 
                    "slidesPerView": 3, 
                    "spaceBetween": 12,
                    "slidesPerGroup": 2 
                  },    
                  "768": { 
                    "slidesPerView": 4, 
                    "spaceBetween": 24, 
                    "slidesPerGroup": 3 
                  },
                  "1200": { 
                    "slidesPerView": 6, 
                    "spaceBetween": 64, 
                    "slidesPerGroup": 3
                  }
                }
              }'>
              <div class="swiper-wrapper">
                {% for product in section.settings.collection_2.products limit: 6 %}
                  <div class="swiper-slide">
                    <div class="wg-cls style-circle hover-img">
                      <a href="{{ product.url }}" class="image img-style d-block">
                        {% if product.featured_image %}
                          <img src="{{ product.featured_image | img_url: 'master' }}"
                               data-src="{{ product.featured_image | img_url: 'master' }}"
                               alt="{{ product.title | escape }}" class="lazyload">
                        {% else %}
                          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
                        {% endif %}
                      </a>
                      <div class="cls-content text-center">
                        <a href="{{ product.url }}" class="link text-md fw-medium">{{ product.title }}</a>
                      </div>
                    </div>
                  </div>
                {% endfor %}
              </div>
              <span class="d-flex d-xl-none sw-dot-default sw-pagination-2 justify-content-center"></span>
            </div>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Categories",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Categories"
    },
    {
      "type": "collection",
      "id": "collection_1",
      "label": "First Collection"
    },
    {
      "type": "collection",
      "id": "collection_2",
      "label": "Second Collection"
    }
  ],
  "presets": [
    {
      "name": "Categories"
    }
  ]
}
{% endschema %} 